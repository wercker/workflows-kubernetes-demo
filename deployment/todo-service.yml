---
kind: Service
apiVersion: v1
metadata:
  name: todo
  labels:
    name: todo
  namespace: ${TPL_ENVIRONMENT}
spec:
  ports:
    - port: 80
      targetPort: http-server
      protocol: TCP
  selector:
    name: todo
